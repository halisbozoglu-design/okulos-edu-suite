import{describe,expect,it}from"bun:test";import{worldCases}from"../tools/schedule_world_export_cases";
describe("world external case export",()=>{it("exports the full 6x30 corpus with stable profile hashes",()=>{const x=worldCases();expect(x.length).toBe(180);expect(new Set(x.map(v=>v.profile_id)).size).toBe(6);for(const p of new Set(x.map(v=>v.profile_id))){const r=x.filter(v=>v.profile_id===p);expect(r.length).toBe(30);expect(new Set(r.map(v=>v.input_hash)).size).toBe(1);expect(r.every(v=>v.schema==="OKULOS_WORLD_BENCHMARK_V1"&&v.wall_clock_budget_ms===8000)).toBe(true)}})});
