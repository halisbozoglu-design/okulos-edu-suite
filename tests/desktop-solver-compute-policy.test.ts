import{describe,expect,it}from'bun:test';import{ASC_INSPIRED_DESKTOP_ENGINE,buildDesktopComputePlan}from'../src/lib/desktop-solver-compute-policy';
describe('desktop solver compute policy',()=>{it('falls back to CPU when GPU is unavailable',()=>{const p=buildDesktopComputePlan({logicalCpuCount:8,gpuAvailable:false},'DEEP');expect(p.backend).toBe('CPU_ONLY');expect(p.cpuWorkers).toBeGreaterThan(0);expect(p.gpuCandidateBatching).toBe(false)});it('uses GPU only as accelerator, never objective authority',()=>{const p=buildDesktopComputePlan({logicalCpuCount:16,gpuAvailable:true},'DEEP');expect(p.backend).toBe('CPU_GPU_HYBRID');expect(p.gpuCandidateBatching).toBe(true);expect(p.gpuObjectiveAuthority).toBe(false);expect(ASC_INSPIRED_DESKTOP_ENGINE.canonicalObjectiveAuthority).toBe('CPU_CANONICAL_SCORER')});it('reserves CPU capacity instead of blindly spawning one worker per logical core',()=>{expect(buildDesktopComputePlan({logicalCpuCount:16,gpuAvailable:false},'BALANCED').cpuWorkers).toBeLessThan(16)})});