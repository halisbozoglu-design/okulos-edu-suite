import{describe,expect,it}from'bun:test';import{decideSync,validateDesktopSyncPolicy,DESKTOP_SYNC_SAFETY,type DesktopSyncPolicy}from'../src/lib/desktop-sync-policy';
const base:DesktopSyncPolicy={direction:'BIDIRECTIONAL',trigger:'MANUAL',modules:['SCHEDULE','RULES'],conflictPolicy:'ASK_USER'};
describe('desktop sync policy',()=>{it('never allows silent overwrite by policy',()=>{expect(DESKTOP_SYNC_SAFETY.silentOverwrite).toBe(false);expect(DESKTOP_SYNC_SAFETY.canonicalServerValidation).toBe(true);expect(DESKTOP_SYNC_SAFETY.auditRequired).toBe(true)});it('detects bidirectional revision conflict',()=>{expect(decideSync(base,{entityType:'schedule',entityId:'1',webRevision:'w2',desktopRevision:'d2',webHash:'A',desktopHash:'B'})).toBe('CONFLICT')});it('supports explicit web to desktop pull',()=>{expect(decideSync({...base,direction:'PULL_WEB_TO_DESKTOP'},{entityType:'schedule',entityId:'1',webRevision:'w2',desktopRevision:'d1',webHash:'A',desktopHash:'B'})).toBe('PULL')});it('requires valid interval and module scope',()=>{expect(()=>validateDesktopSyncPolicy({...base,trigger:'INTERVAL',intervalMinutes:0})).toThrow('SYNC_INTERVAL_INVALID');expect(()=>validateDesktopSyncPolicy({...base,modules:[]})).toThrow('SYNC_MODULES_REQUIRED')})});