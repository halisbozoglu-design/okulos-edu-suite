import { readFile,readdir } from "node:fs/promises";
const read=(path)=>readFile(new URL(`../${path}`,import.meta.url),"utf8");
const[schedule,authority]=await Promise.all([read("src/routes/schedule.tsx"),read("scripts/check-timetable-authority.mjs")]);
for(const token of ['supabase.rpc("upsert_schedule_slot_v2"','supabase.rpc("get_schedule_integrity_report")','manualBlocked'])if(!schedule.includes(token)){console.error(`Manuel program güvenlik zinciri eksik: ${token}`);process.exit(1)}
const migrationDir=new URL("../supabase/migrations/",import.meta.url),files=(await readdir(migrationDir)).filter(x=>x.endsWith(".sql")).sort(),texts=await Promise.all(files.map(async file=>({file,text:await readFile(new URL(file,migrationDir),"utf8")}))),joined=texts.map(x=>x.text).join("\n");
for(const token of ["get_schedule_phase3_preflight_issues_v1","LOCKED_SCOPED_RULE_CONFLICT","SYNC_GROUP_NO_COMMON_WINDOW","get_schedule_phase3_scenario_issues_v1","SCOPED_COURSE_TIME_RULE","SCOPED_COURSE_DAILY_LIMIT","SCOPED_COURSE_MINIMUM_SPREAD","SCOPED_COURSE_BLOCK_PATTERN","LOCKED_SLOT_NOT_PRESERVED","SYNC_GROUP_NOT_PLACED","SYNC_GROUP_PARTIAL_SLOT","schedule_scenario_block_matches_phase3_v1","schedule_current_block_matches_phase3_v1","SCENARIO_HAS_HARD_ISSUES","APPLIED_SCHEDULE_FAILED_FINAL_VALIDATION","PUBLISH_BLOCKED_BY_HARD_ISSUES","guard_schedule_row_tenant_phase3_v1","CROSS_TENANT_SCHEDULE_WRITE","get_schedule_phase3_scoped_preference_score_v1","assert_schedule_scenario_tenant_phase3_v1","SCENARIO_NOT_FOUND_IN_TENANT","REPAIR_WORSENED_HARD_ISSUES"])if(!joined.includes(token)){console.error(`Faz 3 migration sözleşmesi eksik: ${token}`);process.exit(1)}
const authorityPairs={generate_schedule_scenarios_v2:"20260821094620_dd216589-7cc9-42b0-86bd-576c5074d9f7.sql",validate_schedule_scenario_v2:"20260826231000_schedule_generic_constraint_parity.sql",repair_schedule_scenario_v2:"20260821021000_schedule_phase3_rpc_tenant_guards.sql",rescore_schedule_scenario_v2:"20260821021000_schedule_phase3_rpc_tenant_guards.sql",assign_classrooms_to_scenario:"20260821021000_schedule_phase3_rpc_tenant_guards.sql",apply_schedule_scenario:"20260821015500_schedule_phase3_authority_closure.sql",publish_current_schedule:"20260821015500_schedule_phase3_authority_closure.sql",get_schedule_preparation_readiness:"20260821015500_schedule_phase3_authority_closure.sql",get_schedule_scenario_hard_issues_v2:"20260826235000_schedule_room_building_parity_v2.sql",get_schedule_integrity_report:"20260821094548_d494d34a-aceb-44aa-abba-54f1073092b9.sql",calculate_schedule_scenario_score_v2:"20260826124000_schedule_student_conflict_objective_v1.sql"};for(const[name,file]of Object.entries(authorityPairs)){const pattern=new RegExp(`["']function:${name}["']\\s*:\\s*["']${file.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')}["']`);if(!pattern.test(authority)){console.error(`Faz 3 authority kaydı eksik: ${name} -> ${file}`);process.exit(1)}}
const phase3=texts.find(x=>x.file==="20260821015500_schedule_phase3_authority_closure.sql")?.text??"",applyPos=phase3.indexOf("create or replace function public.apply_schedule_scenario"),publishPos=phase3.indexOf("create or replace function public.publish_current_schedule");if(applyPos<0||publishPos<0){console.error("Faz 3 apply/publish authority bulunamadı.");process.exit(1)}const applyBody=phase3.slice(applyPos,publishPos);if(!(applyBody.indexOf("validate_schedule_scenario_v2")<applyBody.indexOf("apply_schedule_scenario_pre_phase3"))){console.error("Apply önce hard validation çalıştırmıyor.");process.exit(1)}if(!applyBody.includes("get_schedule_integrity_report()")){console.error("Apply sonrası final integrity kontrolü eksik.");process.exit(1)}const publishBody=phase3.slice(publishPos);if(!(publishBody.indexOf("get_schedule_integrity_report()")<publishBody.indexOf("publish_current_schedule_pre_phase3"))){console.error("Publish öncesi final integrity gate eksik.");process.exit(1)}
const rpcGuard=texts.find(x=>x.file==="20260821021000_schedule_phase3_rpc_tenant_guards.sql")?.text??"";for(const fn of ["validate_schedule_scenario_v2","repair_schedule_scenario_v2","rescore_schedule_scenario_v2","assign_classrooms_to_scenario","generate_schedule_scenarios_v2"]){const pos=rpcGuard.indexOf(`create or replace function public.${fn}`);if(pos<0){console.error(`Tenant RPC guard eksik: ${fn}`);process.exit(1)}const next=rpcGuard.indexOf("create or replace function public.",pos+20),body=rpcGuard.slice(pos,next<0?undefined:next);if(!body.includes("assert_schedule_scenario_tenant_phase3_v1")&&fn!=="generate_schedule_scenarios_v2"){console.error(`${fn}: scenario tenant assertion eksik.`);process.exit(1)}}
console.log("Faz 3 tamamlık guardı geçti: preflight, scoped hard/soft, blok, locked/sync, tenant RPC, repair, manual edit, apply ve publish tek doğrulama zincirinde.");
