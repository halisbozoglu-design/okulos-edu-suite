import os from"node:os";import{mkdir,readdir,readFile}from"node:fs/promises";import{join}from"node:path";import{tmpdir}from"node:os";import{makeWorldProblem}from"./schedule_world_benchmark";import{IncrementalScheduleScore}from"../src/lib/schedule-local-incremental-score";import type{LocalLockedRow,LocalProblem}from"../src/lib/schedule-local-solver-time-core";
const manifest=await Bun.file(new URL("../benchmarks/world/manifest.json",import.meta.url)).json()as any;const esc=(s:unknown)=>String(s??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;");const tag=(n:string,v:unknown)=>`<${n}>${esc(v)}</${n}>`;const pct=(a:number[],q:number)=>{const x=[...a].sort((m,n)=>m-n);return x[Math.min(x.length-1,Math.max(0,Math.ceil(x.length*q)-1))]??0};
export function buildFetXml(p:LocalProblem){const teachers=[...new Set(p.assignments.map(a=>a.teacher_id))],classes=[...new Set(p.assignments.map(a=>a.class_id))],courses=[...new Set(p.assignments.map(a=>a.course_id))],tc=new Map(p.teacherConstraints.map(x=>[x.teacher_id,x]));return`<?xml version="1.0" encoding="UTF-8"?><fet version="6.0.0"><Institution_Name>Okulos world benchmark</Institution_Name><Comments>Generated common-core benchmark input</Comments><Days_List><Number_of_Days>${p.days.length}</Number_of_Days>${p.days.map(d=>`<Day>${tag("Name",`D${d}`)}</Day>`).join("")}</Days_List><Hours_List><Number_of_Hours>${p.periods}</Number_of_Hours>${Array.from({length:p.periods},(_,i)=>`<Hour>${tag("Name",`H${i+1}`)}</Hour>`).join("")}</Hours_List><Subjects_List>${courses.map(x=>`<Subject>${tag("Name",x)}<Comments/></Subject>`).join("")}</Subjects_List><Activity_Tags_List/><Teachers_List>${teachers.map(x=>`<Teacher>${tag("Name",x)}<Target_Number_of_Hours>0</Target_Number_of_Hours><Qualified_Subjects/><Comments/></Teacher>`).join("")}</Teachers_List><Students_List><Year><Name>OKULOS</Name><Number_of_Students>${classes.length}</Number_of_Students><Comments/>${classes.map(x=>`<Group>${tag("Name",x)}<Number_of_Students>1</Number_of_Students><Comments/></Group>`).join("")}</Year></Students_List><Activities_List>${p.assignments.map((a,i)=>`<Activity>${tag("Teacher",a.teacher_id)}${tag("Subject",a.course_id)}${tag("Students",a.class_id)}<Duration>1</Duration><Total_Duration>1</Total_Duration>${tag("Id",i+1)}<Activity_Group_Id>0</Activity_Group_Id><Active>true</Active>${tag("Comments",a.assignment_id)}</Activity>`).join("")}</Activities_List><Buildings_List/><Rooms_List/><Time_Constraints_List><ConstraintBasicCompulsoryTime><Weight_Percentage>100</Weight_Percentage><Active>true</Active><Comments/></ConstraintBasicCompulsoryTime>${teachers.flatMap(t=>{const c=tc.get(t),z=[] as string[];if(c?.max_daily_hours)z.push(`<ConstraintTeacherMaxHoursDaily><Weight_Percentage>100</Weight_Percentage>${tag("Teacher_Name",t)}${tag("Maximum_Hours_Daily",c.max_daily_hours)}<Active>true</Active><Comments/></ConstraintTeacherMaxHoursDaily>`);if(c?.max_consecutive_hours)z.push(`<ConstraintTeacherMaxHoursContinuously><Weight_Percentage>100</Weight_Percentage>${tag("Teacher_Name",t)}${tag("Maximum_Hours_Continuously",c.max_consecutive_hours)}<Active>true</Active><Comments/></ConstraintTeacherMaxHoursContinuously>`);return z}).join("")}</Time_Constraints_List><Space_Constraints_List><ConstraintBasicCompulsorySpace><Weight_Percentage>100</Weight_Percentage><Active>true</Active><Comments/></ConstraintBasicCompulsorySpace></Space_Constraints_List></fet>`}
const val=(b:string,n:string)=>b.match(new RegExp(`<${n}>([\\s\\S]*?)<\\/${n}>`))?.[1]?.trim()??"";export function parseFetActivities(xml:string,p:LocalProblem){const blocks=[...xml.matchAll(/<Activity>([\s\S]*?)<\/Activity>/g)],out:LocalLockedRow[]=[];for(const m of blocks){const b=m[1]!,id=Number(val(b,"Id")||val(b,"Activity_Id")),a=p.assignments[id-1],day=Number(val(b,"Day").replace(/^D/,"")),period=Number(val(b,"Hour").replace(/^H/,""));if(!a||!Number.isFinite(day)||!Number.isFinite(period))continue;out.push({assignment_id:a.assignment_id,teacher_id:a.teacher_id,class_id:a.class_id,weekday:day,period,schedule_session_id:a.schedule_session_id??null,locked:false,activity_key:`fet:${a.assignment_id}`,activity_duration:1})}return out}
export function auditWorldHard(p:LocalProblem,rows:LocalLockedRow[]){let hard=0;const by=new Map(rows.map(r=>[r.assignment_id,r]));for(let i=0;i<rows.length;i++)for(let j=i+1;j<rows.length;j++){const a=rows[i]!,b=rows[j]!;if(a.weekday===b.weekday&&a.period===b.period&&(a.teacher_id===b.teacher_id||a.class_id===b.class_id))hard++}for(const c of p.teacherConstraints){const rs=rows.filter(r=>r.teacher_id===c.teacher_id);for(const d of p.days){const ps=rs.filter(r=>r.weekday===d).map(r=>r.period).sort((a,b)=>a-b);if(c.max_daily_hours&&ps.length>c.max_daily_hours)hard+=ps.length-c.max_daily_hours;if(c.max_consecutive_hours){let run=0,best=0,last=-99;for(const x of ps){run=x===last+1?run+1:1;last=x;best=Math.max(best,run)}if(best>c.max_consecutive_hours)hard+=best-c.max_consecutive_hours}}}return{hard,unplaced:p.assignments.length-by.size}}
async function findXml(dir:string):Promise<string|null>{for(const n of await readdir(dir,{withFileTypes:true})){const p=join(dir,n.name);if(n.isDirectory()){const x=await findXml(p);if(x)return x}else if(/activities.*\.xml$|activities_timetable\.xml$/i.test(n.name))return p}return null}
async function executable(){const p=Bun.spawn(["sh","-lc","command -v fet-cl"],{stdout:"pipe",stderr:"pipe"});return(await new Response(p.stdout).text()).trim()}
const fetArgs=(exe:string,input:string,dir:string,seed:number)=>[exe,`--inputfile=${input}`,`--outputdir=${dir}`,`--timelimitseconds=${Math.ceil(manifest.wall_clock_budget_ms/1000)}`,"--writetimetablesxml=true",`--randomseedx=${(seed%2147483646)+1}`,`--randomseedy=${((seed*48271)%2147483646)+1}`];
export async function runFetWorldBenchmark(){const exe=await executable();if(!exe)return{schema:manifest.schema,solver:"fet",status:"NOT_RUN",reason:"FET_EXECUTABLE_NOT_FOUND",comparable_objective:false,results:[]};const seeds=Array.from({length:manifest.seed_count},(_,i)=>101+i*7919),results=[] as any[];let xmlOutputs=0;for(const profile of manifest.profiles){const times:number[]=[],hard:number[]=[],unplaced:number[]=[],medium:number[]=[],soft:number[]=[];let feasible=0,replay=true;for(let si=0;si<seeds.length;si++){const seed=seeds[si]!,p=makeWorldProblem(profile,seed),dir=join(tmpdir(),`okulos-fet-${profile.id}-${seed}`);await mkdir(dir,{recursive:true});const input=join(dir,"input.fet");await Bun.write(input,buildFetXml(p));const t=performance.now(),proc=Bun.spawn(fetArgs(exe,input,dir,seed),{stdout:"pipe",stderr:"pipe"});const code=await proc.exited,ms=performance.now()-t;times.push(ms);let rows:LocalLockedRow[]=[];if(code===0){const f=await findXml(dir);if(f){xmlOutputs++;rows=parseFetActivities(await readFile(f,"utf8"),p)}}const a=auditWorldHard(p,rows),s=new IncrementalScheduleScore(p.assignments,rows,p.planningRelations??[],p.studentConflictWeights??[]).score(a.hard,a.unplaced);hard.push(s.hard);unplaced.push(a.unplaced);medium.push(s.medium);soft.push(s.soft);if(s.hard===0&&a.unplaced===0)feasible++;if(si===0){const sig=JSON.stringify(rows.map(r=>[r.assignment_id,r.weekday,r.period]).sort());const d2=join(tmpdir(),`okulos-fet-replay-${profile.id}-${seed}`);await mkdir(d2,{recursive:true});const i2=join(d2,"input.fet");await Bun.write(i2,buildFetXml(p));const q=Bun.spawn(fetArgs(exe,i2,d2,seed),{stdout:"pipe",stderr:"pipe"});if(await q.exited===0){const f2=await findXml(d2);const r2=f2?parseFetActivities(await readFile(f2,"utf8"),p):[];replay=!!f2&&sig===JSON.stringify(r2.map(r=>[r.assignment_id,r.weekday,r.period]).sort())}else replay=false}}
 results.push({profile_id:profile.id,runs:seeds.length,feasible_rate:feasible/seeds.length,hard_max:Math.max(...hard),unplaced_max:Math.max(...unplaced),medium_p50:pct(medium,.5),soft_p50:pct(soft,.5),runtime_p50_ms:Math.round(pct(times,.5)),runtime_p95_ms:Math.round(pct(times,.95)),deterministic_replay:replay,budget_pass:times.every(x=>x<=manifest.wall_clock_budget_ms+1500)})}if(xmlOutputs===0)return{schema:manifest.schema,solver:"fet",status:"NOT_RUN",reason:"FET_XML_OUTPUT_NOT_FOUND",comparable_objective:false,results};return{schema:manifest.schema,solver:"fet",status:"RUN",objective_mapping:"HARD_COMMON_CORE_CANONICAL_POSTSCORE",comparable_objective:false,hardware:{cpu:os.cpus()[0]?.model??"unknown",logical_cpus:os.cpus().length},results}}
if(import.meta.main){const r=await runFetWorldBenchmark(),oi=process.argv.indexOf("--out");console.log("SCHEDULE_FET_WORLD_BENCH",JSON.stringify(r));if(oi>=0&&process.argv[oi+1])await Bun.write(process.argv[oi+1]!,JSON.stringify(r,null,2));if(process.argv.includes("--ci")&&r.status!=="RUN")throw new Error(String(r.reason??"FET_NOT_RUN"))}
